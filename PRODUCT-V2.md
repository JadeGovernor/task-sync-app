# 效率任务 V2 产品设计（定稿 2026-09-08）

> 本文件是 V2 的唯一设计来源，开发与后续对话都以它为准。
> V1（生活/公司/计划 + 今日打卡）将被本设计替换，数据清空重来。

## 1. 产品定位

个人效率工具，Web + iPhone 三端同步（2 台 Mac + 1 台 iPhone，GitHub 私有仓库存数据）。
顶部结构：**今日聚合首页 + 公司 / 计划 / 自律 / 效率 四模块**。

## 2. 已确认决策（逐条来自用户）

| 问题 | 结论 |
|---|---|
| 顶部模块 | 四模块全新布局：公司 / 计划 / 自律 / 效率；原「生活」取消，生活类事项并入自律 |
| 今日首页 | 保留「今日」聚合首页：全模块今天该勾的事汇总打卡 |
| 大条目细节 | **进展备注流水**：大条目下按天追加进展/想法（像日志），不参与打勾 |
| 公司完成 | 大条目手动勾完成；自动顺延：明日→到期当天变今日，今日未完成过夜→次日自动顺延并标红「逾期」 |
| 自律模型 | 两档：**每日习惯**（每天都该勾，漏勾次日自动顺延显示在今天并标红「昨日未打勾」）/ **每周习惯**（自然周内勾一次即完成，周一自动重置） |
| 计划板形态 | 上下双区联动：顶部排序表（自由拖拽）+ 红色「紧急」置顶（进入时间范围最后 10 天自动置顶标红）；下方为全部计划条目列表并在下方新建，上下自动同步同一条 |
| 数据迁移 | 清空重来：按新结构重建数据文件，旧种子/测试数据不迁移 |
| 阶段顺序 | 第一期先做主体（今日 + 公司/计划/自律 + 三端同步）；第二期再做效率模块 |
| 效率机制 | 第二期用系统方案：B 站音频导入 Apple Music 当闹钟铃声（早起到点必响）；早睡用系统提醒 + 自动降低亮度营造氛围 |

## 3. 模块规格

### 3.1 今日（聚合首页）
- 展示：公司条目中「今日到期 + 逾期未勾」+ 自律「每日习惯今日应勾」（含顺延标红项）。
- 打勾行为：一次点击完成；进度显示当日完成率；连续达标天数保留（规则沿用 V1：某天 total=0 视为达标）。

### 3.2 公司
- 两个分组：今日 / 明日（按到期日自动滚动，非手动分组）。
- 条目：大标题 + 截止日 + 进展备注流水（可多次追加，按日期倒序/正序展示）。
- 完成：勾「完成」；逾期未勾的自动顺延到今天顶部红色「逾期」。

### 3.3 计划
- 大条目：计划方向 + 可输入的**时间范围**（开始日~结束日）+ 进展备注流水。
- 页面上下双区联动：
  - 顶部：排序表卡片，可**自由拖拽排序**（顺序跨设备同步）；进入最后 10 天的条目自动标红并置顶到「紧急」区。
  - 下方：全部计划条目列表，新建入口在此；写一条上面自动同步出现同一条。
- 排序优先级：紧急(最后10天) > 拖拽顺序（紧急区内部也可拖）。

### 3.4 自律
- 分成「日打勾」「周打勾」两块视图（可切换或分区）。
- 每日习惯：默认每天都该勾；昨天漏勾 → 今天显示红色「昨日未打勾」，打勾仍记今天。
- 每周习惯：自然周（周一起）内任意一天勾一次算完成，显示本周已勾；周一自动重置。
- 生活类自律（作息/运动/阅读等）都放这里。

### 3.5 效率（第二期，本期只留入口占位）
- 早起：设置时间（默认 06:30）+ 关联 B 站链接；Mac 上 `scripts/download_bilibili.sh` 下载转 mp3 → 导入 Apple Music（同一 Apple ID 自动同步到 iPhone）→ iPhone「时钟」闹钟用该音频当铃声。
- 早睡：设置时间（默认 22:30）；到点系统提醒，可选快捷指令调暗亮度，营造褪黑素氛围；睡前播英语播客（Apple Podcasts）。
- 本期范围：设置页保留早起/早睡时间与开关（沿用 V1 settings），音频集成第二期做。

## 4. 数据结构草案（V2，清空重来）

沿用 GitHub 私有仓库 3 个 JSON 的思路，文件内容按新结构重建：

- `tasks.json` → 大条目数组，每项字段：
  - 通用：`id`、`kind`(`work`|`plan`|`habit`)、`title`、`created`、`updatedAt`
  - 公司：`due`(YYYY-MM-DD)、`done`、`doneDate`
  - 计划：`rangeStart`、`rangeEnd`、`rank`(拖拽排序值)
  - 自律：`freq`(`daily`|`weekly`)、（每周习惯的完成按周记录）
  - 备注流水：`notes: [{date, text}]`（三类大条目通用）
- `checkins.json` → 按日期打卡（公司一次性 + 每日习惯）+ 每周习惯完成记录（周 key）。
- `settings.json` → 每设备闹钟/名称等（沿用 V1 结构）。

> 详细字段在实现时以本草案为准细化；拖拽顺序、每周完成、备注流水三者要能在三端合并不丢。

## 5. 阶段与验收

**第一期（主体）**：PWA 重构为「今日 + 公司/计划/自律 + 设置」；数据结构重建；三端同步沿用 GitHub Contents API + 离线队列。
- 验收：Mac A 新建公司「明日」条目 → Mac B / iPhone 可见；明日到期自动滚到今日；不勾过夜标红逾期；自律每日漏勾次日红字顺延；计划拖拽排序与紧急置顶三端一致；离线改动联网后自动补传。

**第二期（效率）**：B 站音频下载/导入流程 + iPhone 闹钟/提醒实操手册；设置页联动。
- 验收：iPhone 闹钟实测到点响（先设 2 分钟后验证）；早睡提醒到点弹出。

## 6. 待办开放项（不阻塞开发，遇分歧先按默认走）
- 每日习惯漏勾是否允许「补勾昨日」（默认：只记今天，昨日仅红字提示）。
- 计划的完成态：方向结束即归档，还是手动标记完成（默认：时间范围结束自动归档进历史，可手动恢复）。
